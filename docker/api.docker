FROM ubuntu:16.04

RUN apt-get update

RUN apt-get install \
    build-essential \
    curl \
    vim -y

RUN curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.31.7/install.sh | bash

ENV NODE_VERSION 6.3

RUN bash -c "source /root/.nvm/nvm.sh && nvm install $NODE_VERSION"
